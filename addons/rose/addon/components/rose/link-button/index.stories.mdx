import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { action } from '@storybook/addon-actions';
import { text, boolean, select } from '@storybook/addon-knobs';

<Meta title="Rose|LinkButton" component="LinkButton" />

# LinkButton

An achor element component styled to mimic `Rose::Button` component.
Styles supported: primary, secondary, warning, and ghost.
Disabling link button is not supported as it is an action on form components only.

```html
<Rose::LinkButton @route="index" @style="primary" @iconRight="chevron-right">
  Content
</Rose::LinkButton>
```

<Preview>
  <Story name="Advanced">{{
    template: hbs`
      <Rose::LinkButton
        @route="index"
        @style={{style}}
        @iconLeft={{iconLeft}}
        @iconRight={{iconRight}}
      >
        {{text}}
      </Rose::LinkButton>
    `,
    context: {
      text: text('text', 'Content'),
      style: select(
        'style',
        {
          primary: 'primary',
          secondary: 'secondary',
          warning: 'warning',
          ghost: 'ghost',
          'inline-link-action': 'inline-link-action',
        },
        'primary'
      ),
      iconLeft: select(
        'iconLeft',
        {
          'check-circle-outline': 'check-circle-outline',
          'flight/clipboard-copy-16': 'flight/clipboard-copy-16',
          'unfold-more': 'unfold-more',
        },
        'flight/clipboard-copy-16'
      ),
      iconRight: select(
        'iconRight',
        {
          'check-circle-outline': 'check-circle-outline',
          'chevron-right': 'chevron-right',
          'flight/clipboard-copy-16': 'flight/clipboard-copy-16',
          'search': 'search',
        },
        'chevron-right'
      ),
    },
  }}</Story>
</Preview>

Similar to `Rose::Button`, icon only link button is supported.

```html
<Rose::LinkButton @route="index" @style="primary" @iconOnly="flight/help-16" />
```

<Preview>
  <Story name="Icon Only">{{
    template: hbs`
    <Rose::LinkButton @route="" @style={{style}} @iconOnly={{iconOnly}} />
    `,
    context: {
      style: select(
        'style',
        {
          primary: 'primary',
          secondary: 'secondary',
          warning: 'warning',
          ghost: 'ghost',
          'inline-link-action': 'inline-link-action',
        },
        'primary'
      ),
      iconOnly: select(
        'iconOnly',
        {
          'check-circle-outline': 'check-circle-outline',
          'flight/help-16': 'flight/help-16',
          'plus-circle-fill': 'plus-circle-fill',
          'user-square-outline': 'user-square-outline',
        },
        'flight/help-16'
      ),
    },
  }}</Story>
</Preview>
