import Component from '@ember/component';
import layout from '../../../templates/components/rose/nav/breadcrumbs';

/**
 * A navigation component with links.
 */
export default Component.extend({
  layout,
  tagName: '',
});
