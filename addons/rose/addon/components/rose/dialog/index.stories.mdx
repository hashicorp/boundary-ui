
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { action } from '@storybook/addon-actions';
import { text, boolean, select } from '@storybook/addon-knobs';

<Meta title="Rose|Dialog" component="Dialog" />

# Dialog

A dialog component with optional modal capabilities.

<Preview>
  <Story name="Dialog" height="15rem">{{
    template: hbs`
      <Rose::Dialog
        @modal={{modal}}
        @dismiss={{dismiss}}
        @icon={{icon}}
        @heading={{heading}}
        @dismissButtonText={{dismissButtonText}}
        @style={{style}}
      as |dialog|>
        <dialog.body>
          Dialog Body
        </dialog.body>
        <dialog.footer>
          Footer
        </dialog.footer>
      </Rose::Dialog>
    `,
    context: {
      modal: boolean('modal', false),
      heading: text('heading', 'Dialog Heading Text'),
      dismissButtonText: text('dismissButtonText', 'Dismiss'),
      icon: select(
        'icon',
        {
          'flight/check-circle-16': 'flight/check-circle-16',
          'flight/clipboard-copy-16': 'flight/clipboard-copy-16',
        },
        'flight/check-circle-16'
      ),
      style: select(
        'style',
        {
          'default': '',
          'warning': 'warning'
        }
      ),
      dismiss: action('dismiss')
    }
  }}</Story>
</Preview>


```htmlbars
<Rose::Dialog
  @modal={{true}}
  @dismiss={{@dismissFn}}
  @icon="flight/check-circle-16"
  @heading="Heading"
  @dismissButtonText="Dismiss"
as |dialog|>
  <dialog.body>
    Dialog Body
  </dialog.body>
  <dialog.footer>
    Footer
  </dialog.footer>
</Rose::Dialog>
```
