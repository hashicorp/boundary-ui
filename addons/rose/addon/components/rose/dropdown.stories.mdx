import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { action } from '@storybook/addon-actions';
import { text, boolean, select } from '@storybook/addon-knobs';

<Meta title="Rose|Dropdown" component="Dropdown" />

# Dropdown

A simple dropdown component with support for links and buttons. Supports `light` and `dark` styles, with latter being a default style.

<Preview>
  <Story name="Basic Dropdown" height="5rem">{{
    template: hbs`
      <Rose::Dropdown @text="Choose One&hellip;" @icon="user-organization" @style={{style}} as |dropdown|>
        <dropdown.link @route="index">Menu item</dropdown.link>
        <dropdown.link @route="about">Menu item</dropdown.link>
        <dropdown.button>Button menu item</dropdown.button>
      </Rose::Dropdown>
    `,
    context: {
      style: select(
        'style',
        {
          'light': 'light',
          'dark': 'dark',
        },
        'dark'
      ),
    }
  }}</Story>
</Preview>

<Preview>
  <Story name="Icon-only Dropdown" height="5rem">{{
    template: hbs`
      <Rose::Dropdown @text="Choose One&hellip;" @icon="user-organization" @iconOnly={{true}} as |dropdown|>
        <dropdown.link @route="index">Menu item</dropdown.link>
        <dropdown.link @route="about">Menu item</dropdown.link>
        <dropdown.button>Button menu item</dropdown.button>
      </Rose::Dropdown>
    `,
    context: {}
  }}</Story>
</Preview>


```htmlbars
  <Rose::Dropdown @text="Choose One&hellip;" as |dropdown|>
    <dropdown.link @route="index">Menu item</dropdown.link>
    <dropdown.link @route="about">Menu item</dropdown.link>
    <dropdown.button>Button menu item</dropdown.button>
  </Rose::Dropdown>
```

Dropdown content can be aligned to right edge of the component instead of the default left edge, which is the default setting. The following example floats the component to right edge of the page inorder to provide enough space to display right aligned dropdown content.

```htmlbars
  <Rose::Dropdown @text="Choose One&hellip;" style="float: right" @alignRight={{true}} as |dropdown|>
    <dropdown.link @route="index">Menu item</dropdown.link>
    <dropdown.link @route="about">Menu item</dropdown.link>
    <dropdown.button>Button menu item</dropdown.button>
  </Rose::Dropdown>
```

Dropdown button styles support all `Rose::Button` styles through a `style` attribute.

```htmlbars
<Rose::Dropdown @text="Dropdown" as |dropdown|>
  <dropdown.button>Button menu item</dropdown.button>
  <dropdown.button @style="warning">Button - warning</dropdown.button>
  <dropdown.button @style="primary">Button - primary</dropdown.button>
  <dropdown.button @style="secondary">Button - secondary</dropdown.button>
  <dropdown.button @style="ghost">Button - ghost</dropdown.button>
</Rose::Dropdown>
```
