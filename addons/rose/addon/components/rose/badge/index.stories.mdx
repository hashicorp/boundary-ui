import { Meta, Story, Preview } from '@storybook/addon-docs';
import { hbs } from 'ember-cli-htmlbars';

<Meta title="Rose/Badge (removed)" component="Badge" />

# Rose Badge (Removed)

Rose/Badge has been removed in favor of [HDS::Badge](https://design-system-components-hashicorp.vercel.app/components/badge)

```handlebars
<Hds::Badge @text='Success badge' @color='neutral' />
```

Refer to the [HDS Badge documentation](https://design-system-components-hashicorp.vercel.app/components/badge) for a complete overview

export const HDSTemplate = (args) => ({
  template: hbs`<Hds::Badge @text={{text}}
      @color={{color}}
      />`,
  context: args,
});

<Preview>
  <Story
    name="Hds Badge"
    args={{
      text: 'Badge',
    }}
    argTypes={{
      color: {
        control: 'select',
        options: ['success', 'warning', 'critical', 'highlight'],
      },
    }}
  >
    {HDSTemplate.bind({})}
  </Story>
</Preview>
