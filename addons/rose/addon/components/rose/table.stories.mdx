import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { action } from '@storybook/addon-actions';
import { text, boolean, select } from '@storybook/addon-knobs';

<Meta title="Rose|Table" component="Table" />

# Table

A table component to display data in structure of rows and columns. It supports `header`, `body` and `footer` optional definitions. Each definition allows for a table row to be defined with table cells.

```htmlbars
<Rose::Table as |table|>
  <table.header as |header|>
    <header.row as |row|>
      <row.cell>Name</row.cell>
      <row.cell>Description</row.cell>
    </header.row>
  </table.header>
  <table.body as |body|>
    <body.row as |row|>
      <row.cell>Generated text</row.cell>
      <row.cell>Lorem ipsum dolor sit amet</row.cell>
    </body.row>
  </table.body>
  <table.footer as |footer|>
    <footer.row as |row|>
      <row.cell>Total</row.cell>
      <row.cell>2</row.cell>
    </footer.row>
  </table.footer>
</Rose::Table>
```

<Preview>
  <Story name="Basic">{{
    template: hbs`
    <Rose::Table as |table|>
      <table.header as |header|>
        <header.row as |row|>
          <row.cell
            @iconLeft={{iconLeft}}
            @iconRight={{iconRight}}
          >Header Cell</row.cell>
          <row.cell>Header Cell</row.cell>
        </header.row>
      </table.header>
      <table.body as |body|>
        <body.row as |row|>
          <row.cell>Body cell</row.cell>
          <row.cell>Body cell</row.cell>
        </body.row>
      </table.body>
      <table.footer as |footer|>
        <footer.row as |row|>
          <row.cell>Footer cell</row.cell>
          <row.cell>Footer cell</row.cell>
        </footer.row>
      </table.footer>
    </Rose::Table>
    `,
    context: {
      iconLeft: select(
        'iconLeft',
        {
          'user-square-outline': 'user-square-outline',
          'chevron-left': 'chevron-left',
        },
        'user-square-outline'
      ),
      iconRight: select(
        'iconRight',
        {
          'chevron-right': 'chevron-right',
          'swap-vertical': 'swap-vertical',
        },
        'swap-vertical'
      ),
    }
  }}</Story>
</Preview>
