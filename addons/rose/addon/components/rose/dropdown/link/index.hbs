{{#if @model}}
  <LinkTo
    ...attributes
    @route={{@route}}
    @query={{@query}}
    @model={{@model}}
    class="rose-dropdown-link {{if  @style (concat "rose-dropdown-link-" @style)}}"
  >
    {{yield}}
  </LinkTo>
{{else}}
  <LinkTo
    ...attributes
    @route={{@route}}
    @query={{@query}}
    class="rose-dropdown-link {{if  @style (concat "rose-dropdown-link-" @style)}}"
  >
    {{yield}}
  </LinkTo>
{{/if}}
