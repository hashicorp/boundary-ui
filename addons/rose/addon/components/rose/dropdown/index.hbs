<details
  ...attributes
  class="rose-dropdown {{if @dropdownRight "rose-dropdown-right"}}"
  {{on-click-outside this.close}}
>

  <summary
    class="rose-dropdown-trigger {{if @iconOnly "has-icon-only"}}"
    title={{@text}}
  >

    {{#if @icon}}
      <span class="rose-dropdown-trigger-icon">
        <Rose::Icon
          @name={{@icon}}
          @size="medium"
        />
      </span>
    {{/if}}

    <span class="rose-dropdown-trigger-text">
      {{@text}}
    </span>

    {{#if this.showCaret}}
      <span class="rose-dropdown-trigger-caret">
        <Rose::Icon
          @name="chevron-down"
          @size="medium"
        />
      </span>
    {{/if}}

  </summary>

  <div class="rose-dropdown-content" {{on-click-inside this.close}}>
    {{yield (hash
      link=(component "rose/dropdown/link" style=this.style)
      button=(component "rose/dropdown/button" style=this.style)
      separator=(component "rose/anonymous" tagName="hr" class="rose-dropdown-separator")
    )}}
  </div>

</details>
