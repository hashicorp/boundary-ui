import Component from '@ember/component';
import layout from '../../../templates/components/rose/table/footer';

/**
 * A table footer component that uses tfoot table element.
 */
export default Component.extend({
  layout,
  tagName: '',
});
