import Component from '@ember/component';
import layout from '../../../templates/components/rose/table/header';

/**
 * A table header component that uses thead table element.
 */
export default Component.extend({
  layout,
  tagName: '',
});
