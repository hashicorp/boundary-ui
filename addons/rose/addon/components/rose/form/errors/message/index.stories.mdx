import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { action } from '@storybook/addon-actions';
import { text, boolean } from '@storybook/addon-knobs';

<Meta title="Rose|Form/Errors/Message" component="Form/Errors/Message" />

# Form Error Message

This component accepts block content describing a single error to the user.
Normally this component would not be used directly, but rather in the context
of the `Rose::Form::Errors` component.

```html
<Rose::Form::Errors::Message>
  An error message goes here.
</Rose::Form::Errors::Message>
```

<Preview>
  <Story name="Basic Form Error Message">{{
    template: hbs`
      <Rose::Form::Errors::Message>
        An error message goes here.
      </Rose::Form::Errors::Message>
    `,
    context: {},
  }}</Story>
</Preview>
