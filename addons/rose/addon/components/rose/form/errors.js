import Component from '@ember/component';
import layout from '../../../templates/components/rose/form/errors';

/**
 * A container for error messages.  Yields the error message component.
 */
export default Component.extend({
  layout,
  tagName: ''
});
