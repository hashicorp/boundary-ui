import { Meta, Story, Preview } from '@storybook/addon-docs';
import { hbs } from 'ember-cli-htmlbars';

<Meta title="Rose/Form/Fieldset" component="Form/Fieldset" />

# Form Fieldset

Renders a simple fieldset with title, description and body and an optional helperText

```handlebars
<Rose::Form::Fieldset @helperText={{helperText}}>
  <:title>Title</:title>
  <:description>Description</:description>
  <:body>Rest of the form</:body>
</Rose::Form::Fieldset>
```

export const Template = (args) => ({
  template: hbs`
      <Rose::Form::Fieldset @helperText={{helperText}}>
        <:title>Title</:title>
        <:description>Description</:description>
        <:body>Rest of the form</:body>
      </Rose::Form::Fieldset>`,
  context: args,
});

<Preview>
  <Story
    name="Basic Fieldset"
    args={{
      helperText: 'This is an optional help text.',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>
