{{!--
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: MPL-2.0
--}}

{{#each @items as |item|}}
  {{yield
    (hash
      item=item
      checkbox=(component
        'rose/form/checkbox'
        name=@name
        disabled=@disabled
        inline=@inline
        checked=(call (fn this.hasSelectedItem item))
        onChange=(fn this.toggleItem item)
      )
    )
  }}
{{/each}}