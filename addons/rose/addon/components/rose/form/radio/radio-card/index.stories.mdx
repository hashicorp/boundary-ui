import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { hbs } from 'ember-cli-htmlbars';
import { text, boolean, select } from '@storybook/addon-knobs';

<Meta title="Rose|Form/Radio/Radio/RadioCard" component="Form/Radio/Radio/RadioCard" />

# Form Radiofield

Renders a form radiofield.

```htmlbars
<Rose::Form::Radio::RadioCard::Wide
  @label="Yes"
  @error={{false}}
  @value="yes-agree"
  @selectedValue="yes-agree"
  @disabled={{false}}
  @changed={{fn}}
  />
```

<Preview>
  <Story name="Wide" height="10rem">{{
    template: hbs`
      <Rose::Form::Radio::RadioCard::Wide
        @label={{label}}
        @error={{error}}
        @value={{value}}
        @selectedValue={{selectedValue}}
        @disabled={{disabled}}
        @icon={{icon}}
        />
    `,
    context: {
      label: text('text', 'AWS'),
      error: boolean('error', false),
      value: text('value', 'value and selectedValue must match to select radiofield'),
      selectedValue: text(
        'selectedValue',
        'value and selectedValue must match to select radiofield'
      ),
      disabled: boolean('disabled', false),
      icon: select(
        'icon',
        {
          'none': null,
          'flight-icons/svg/org-16': 'flight-icons/svg/org-16',
          'flight-icons/svg/clipboard-copy-16': 'flight-icons/svg/clipboard-copy-16',
        },
        null
      ),
    }
  }}</Story>
</Preview>


```htmlbars
<Rose::Form::Radio::RadioCard::Tile
  @label="Yes"
  @error={{false}}
  @value="yes-agree"
  @selectedValue="yes-agree"
  @disabled={{false}}
  @changed={{fn}}
  />
```

<Preview>
  <Story name="Tile" height="10rem">{{
    template: hbs`
      <Rose::Form::Radio::RadioCard::Tile
        @label={{label}}
        @error={{error}}
        @value={{value}}
        @selectedValue={{selectedValue}}
        @disabled={{disabled}}
        @icon={{icon}}
        />
    `,
    context: {
      label: text('text', 'AWS Tile'),
      error: boolean('error', false),
      value: text('value', 'value and selectedValue must match to select radiofield'),
      selectedValue: text(
        'selectedValue',
        'value and selectedValue must match to select radiofield'
      ),
      disabled: boolean('disabled', false),
      icon: select(
        'icon',
        {
          'none': null,
          'flight-icons/svg/org-16': 'flight-icons/svg/org-16',
          'flight-icons/svg/clipboard-copy-16': 'flight-icons/svg/clipboard-copy-16',
        },
        null
      ),
    }
  }}</Story>
</Preview>
