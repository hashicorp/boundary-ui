<div class="rose-form-radio{{if @error " error"}} {{if @inline "inline"}}">

  <RadioButton
    ...attributes
    @radioClass="rose-form-radio-field"
    @radioId={{this.id}}
    type="radio"
    disabled={{@disabled}}
    @name={{@name}}
    @value={{@value}}
    @groupValue={{@selectedValue}}
    @disabled={{@disabled}}
    @changed={{@changed}} />

  <label class="rose-form-radio-label" for={{this.id}}>
     {{#if @helperText}}
      <Rose::Form::HelperText @id="helper-text-{{this.id}}" @error={{@error}} @link={{@link}} @linkText={{@linkText}}>
        {{@helperText}}
      </Rose::Form::HelperText>
    {{/if}}
    {{#if @icon}}
      <Rose::Icon
        @name={{@icon}}
        @size="medium"
      />
    {{/if}}

    {{@label}}

  </label>

  {{yield}}

</div>
