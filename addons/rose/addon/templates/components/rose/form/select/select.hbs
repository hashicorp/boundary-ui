<XSelect
  class="rose-form-select-field"
  id={{@id}}
  name={{@name}}
  disabled={{@disabled}}
  @value={{@value}}
  @onChange={{@onChange}}
  aria-describedby="{{if @helperText (concat 'helper-text-' this.id)}}"
  as |xs|
>
  {{yield xs}}
</XSelect>
