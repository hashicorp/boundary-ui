<div class="rose-form-input{{if @error " error"}}">

  <label class="rose-form-input-label" for={{@id}}>
    {{@label}}
  </label>

  {{#if @helperText}}
    <span class="rose-form-input-helper-text">{{@helperText}}</span>
  {{/if}}

  <Textarea
    ...attributes
    class="rose-form-input-field"
    id={{@id}}
    disabled={{@disabled}}
    @value={{@value}} />

  {{yield}}

</div>
