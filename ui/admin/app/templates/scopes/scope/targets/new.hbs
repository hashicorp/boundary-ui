{{page-title (t 'titles.new')}}

<Rose::Layout::Page as |page|>

  <page.breadcrumbs>
    <BreadCrumbs />
  </page.breadcrumbs>

  <page.header>
    <h2>
      {{t 'resources.target.titles.new'}}
      <DocLink @doc='target.new' @iconSize='large' />
    </h2>
    <p>{{t 'resources.target.description'}}</p>
  </page.header>

  <page.body>
    <Form::Target
      @model={{@model}}
      @submit={{route-action 'saveWithAddress' @model}}
      @cancel={{route-action 'cancel' @model}}
      @changeType={{route-action 'changeType'}}
    />
  </page.body>

</Rose::Layout::Page>