{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: BUSL-1.1
}}

<Hds::ApplicationState @align='center' as |A|>
  <A.Media>
    {{! We are not using the <Image> component here due to a CSP violation caused by the use of inline styles on this image. }}
    <img src='/core/auth-pending.svg' alt='' role='none' />
  </A.Media>
  <A.Header @title={{t 'resources.auth-method.messages.pending.title'}} />
  <A.Body @text={{t 'resources.auth-method.messages.pending.description'}} />
  <A.Footer as |F|>
    <F.Button
      @text={{t 'resources.auth-method.actions.no-new-window-retry'}}
      @color='primary'
      {{on 'click' this.retryAuthentication}}
    />
    <F.Button
      @text={{t 'actions.cancel'}}
      @color='secondary'
      @route='scopes.scope.authenticate.method'
    />
  </A.Footer>
</Hds::ApplicationState>