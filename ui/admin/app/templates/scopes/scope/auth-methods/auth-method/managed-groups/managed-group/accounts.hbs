{{page-title (t 'resources.account.title_plural')}}

{{#if @model}}
  <Rose::Table as |table|>
    <table.header as |header|>
      <header.row as |row|>
        <row.headerCell>{{t 'form.name.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.issuer.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.subject.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.email.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.full_name.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.type.label'}}</row.headerCell>
        <row.headerCell>{{t 'form.id.label'}}</row.headerCell>
      </header.row>
    </table.header>
    <table.body as |body|>
      {{#each @model as |account|}}
        <body.row as |row|>
          <row.headerCell scope='row'>
            {{account.accountName}}
            <p>{{account.description}}</p>
          </row.headerCell>
          <row.cell>
            <code>{{account.attributes.issuer}}</code>
          </row.cell>
          <row.cell>
            <code>{{account.attributes.subject}}</code>
          </row.cell>
          <row.cell>
            <code>{{account.attributes.email}}</code>
          </row.cell>
          <row.cell>
            {{account.attributes.full_name}}
          </row.cell>
          <row.cell>
            <Rose::Badge>{{account.type}}</Rose::Badge>
          </row.cell>
          <row.cell>
            <Copyable
              @text={{account.id}}
              @buttonText={{t 'actions.copy-to-clipboard'}}
              @acknowledgeText={{t 'states.copied'}}
            >
              <code>{{account.id}}</code>
            </Copyable>
          </row.cell>
        </body.row>
      {{/each}}
    </table.body>
  </Rose::Table>
{{else}}
  <Rose::Layout::Centered>
    <Rose::Message
      @title={{t 'resources.managed-group.messages.accounts.title'}}
      as |message|
    >
      <message.description>
        {{t 'resources.managed-group.messages.accounts.description'}}
      </message.description>
    </Rose::Message>
  </Rose::Layout::Centered>
{{/if}}