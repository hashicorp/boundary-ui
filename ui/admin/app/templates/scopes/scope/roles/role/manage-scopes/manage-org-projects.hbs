{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: BUSL-1.1
}}

{{page-title
  (t
    'resources.role.scope.messages.manage-org-projects.title'
    orgDisplayName=@model.orgScope.displayName
  )
}}
{{#if @model.role.scope.isGlobal}}
  <Breadcrumbs::Item
    @text={{t 'resources.role.scope.messages.manage-custom-scopes.title'}}
    @route='scopes.scope.roles.role.manage-scopes.manage-custom-scopes'
  />
{{/if}}
<Breadcrumbs::Item
  @text={{t
    'resources.role.scope.messages.manage-org-projects.title'
    orgDisplayName=@model.orgScope.displayName
  }}
  @route='scopes.scope.roles.role.manage-scopes.manage-org-projects'
  @model={{@model}}
/>

<Rose::Layout::Page as |page|>

  <page.breadcrumbs>
    <Breadcrumbs::Container />
  </page.breadcrumbs>

  <page.header>
    <h2>
      {{t
        'resources.role.scope.messages.manage-org-projects.title'
        orgDisplayName=@model.orgScope.displayName
      }}
      <DocLink @doc='role.add-grant-scopes' @iconSize='large' />
    </h2>
    <p>{{t 'resources.role.scope.messages.manage-org-projects.description'}}</p>
  </page.header>

  <page.body class='search-filtering'>
    <Form::Role::ManageOrgProjects
      @model={{@model}}
      @search={{this.search}}
      @page={{this.page}}
      @pageSize={{this.pageSize}}
      @selectedItems={{this.selectedItems}}
      @handleSearchInput={{this.handleSearchInput}}
      @submit={{fn this.setGrantScopes @model.role @model.selectedProjectIDs}}
      @cancel={{fn this.cancel @model.role @model.selectedProjectIDs}}
    />
  </page.body>

</Rose::Layout::Page>