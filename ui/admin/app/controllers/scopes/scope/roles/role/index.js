import Controller, { inject as controller } from '@ember/controller';

export default class ScopesScopeRolesRoleIndexController extends Controller {
  @controller('scopes/scope/roles/index') roles;
}
