import Controller, { inject as controller } from '@ember/controller';

export default class ScopesScopeGroupsGroupMembersController extends Controller {
  @controller('scopes/scope/groups/index') groups;
}
