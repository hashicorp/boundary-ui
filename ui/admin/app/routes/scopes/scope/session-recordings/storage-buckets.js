import Route from '@ember/routing/route';

export default class ScopesScopeSessionRecordingsStorageBucketsRoute extends Route {
  // =services

  // TODO: store, can, and router services

  // =methods

  /**
   * Loads all storage buckets under global and org scopes.
   * @returns {Promise[StorageBucketModel]}
  */
  // TODO: async model hook

  // =actions

  // TODO: save, cancel, and delete actions
}
