{{!
  Copyright IBM Corp. 2021, 2026
  SPDX-License-Identifier: BUSL-1.1
}}

<Rose::Form
  @onSubmit={{this.save}}
  @cancel={{@cancel}}
  @disabled={{@model.isSaving}}
  @showEditToggle={{false}}
  as |form|
>
  <Form::Field::KeyValue
    @name='api_tags'
    @data={{@apiTags}}
    @model={{this}}
    @properties={{array 'key' 'value'}}
    @onAdd={{this.addApiTag}}
    @onRemove={{this.removeApiTagByIndex}}
    @onUpdate={{this.updateApiTagValue}}
    @isOptional={{true}}
    @disabled={{form.disabled}}
  >
    <:row as |R|>
      <R.Field as |F|>
        <F.Label>{{t 'form.key.label'}}</F.Label>
        <F.TextInput
          data-test-key-input
          @value={{R.rowData.key}}
          {{on 'input' (fn R.updateRow 'key')}}
        />
      </R.Field>

      <R.Field as |F|>
        <F.Label>{{t 'form.value.label'}}</F.Label>
        <F.TextInput
          data-test-value-input
          {{on 'input' (fn R.updateRow 'value')}}
        />
      </R.Field>
      {{#if R.canDelete}}
        <R.DeleteRowButton data-test-delete-button @onClick={{R.removeRow}} />
      {{/if}}
    </:row>
  </Form::Field::KeyValue>

  <form.actions
    @submitText={{t 'actions.save'}}
    @cancelText={{t 'actions.cancel'}}
  />
</Rose::Form>