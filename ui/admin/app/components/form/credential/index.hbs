{{#if @model.type}}
  {{component
    (concat 'form/credential/' @model.type)
    model=@model
    types=this.credentialTypes
    submit=@submit
    cancel=@cancel
    changeType=@changeType
  }}
{{/if}}