<Rose::Form
  class='full-width'
  @cancel={{@cancel}}
  @disabled={{@model.isSaving}}
  @onSubmit={{fn @submit this.targetAddress this.targetPort}}
  as |form|
>

  <form.checkbox
    @label={{t 'onboarding.quick-start.titles.toggle-resource-info'}}
    @onChange={{this.toggleHostInfoSetup}}
  />
  {{#if this.showHostInfo}}
    <div class='create-resource-cards'>
      <form.input
        @name='targetAddress'
        @type='text'
        @value={{this.targetAddress}}
        @label={{t 'onboarding.quick-start.titles.target-address'}}
        @helperText={{t 'onboarding.quick-start.descriptions.target-address'}}
      />

      <form.input
        @name='targetPort'
        @type='text'
        @value={{this.targetPort}}
        @label={{t 'onboarding.quick-start.titles.target-port'}}
        @helperText={{t 'onboarding.quick-start.descriptions.target-port'}}
      />
    </div>
  {{/if}}
  <form.fieldset>
    <:title>{{t 'onboarding.quick-start.titles.resource-setup-info'}}</:title>
    <:description>{{t
        'onboarding.quick-start.descriptions.resource-setup-info'
      }}
    </:description>

    <:body>
      <Image @name='resource-creation' />

    </:body>
  </form.fieldset>

  <form.actions @submitText={{t 'actions.next'}} @showCancel={{false}} />
</Rose::Form>