{{#if @model.type}}
  {{component
    (concat 'form/target/' @model.type)
    model=@model
    submit=@submit
    submitWithFilters=@submitWithFilters
    cancel=@cancel
    changeType=@changeType
  }}
{{/if}}