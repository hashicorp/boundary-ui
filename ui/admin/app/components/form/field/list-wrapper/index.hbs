{{!
  Copyright IBM Corp. 2021, 2026
  SPDX-License-Identifier: BUSL-1.1
}}

<Hds::Form::Fieldset
  @layout={{@layout}}
  @isRequired={{@isRequired}}
  @isOptional={{@isOptional}}
  as |F|
>

  {{yield F to='fieldset'}}

  <F.Control>
    {{yield
      (hash
        KeyValue=(component
          'form/field/list-wrapper/key-value' disabled=@disabled
        )
        TextInput=(component
          'form/field/list-wrapper/text-input' disabled=@disabled
        )
        Textarea=(component
          'form/field/list-wrapper/textarea' disabled=@disabled
        )
        Select=(component 'form/field/list-wrapper/select' disabled=@disabled)
      )
      to='field'
    }}
  </F.Control>

</Hds::Form::Fieldset>