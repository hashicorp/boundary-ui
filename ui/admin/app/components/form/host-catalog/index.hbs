{{#if @model.compositeType}}
  {{component
    (concat 'form/host-catalog/' @model.compositeType)
    model=@model
    submit=@submit
    cancel=@cancel
    changeType=@changeType
  }}
{{/if}}

{{{log @model 'modede'}}}