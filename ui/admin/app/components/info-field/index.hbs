<div class='info-field'>
  <Hds::Form::TextInput::Field @value={{@value}} readonly={{true}} as |F|>
    {{#if @icon}}
      <F.HelperText>
        <FlightIcon @name={{@icon}} />
      </F.HelperText>
    {{/if}}
    {{yield F}}
  </Hds::Form::TextInput::Field>
</div>