<Rose::Dropdown @text="{{t 'actions.manage'}}" @dropdownRight={{true}} as |dropdown|>
  <dropdown.button
    @style="danger"
    @disabled={{@model.canSave}}
    {{on "click" (route-action "deleteAccount" @model)}}
  >
    {{t 'actions.delete'}}
  </dropdown.button>
</Rose::Dropdown>
