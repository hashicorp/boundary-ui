<Rose::Layout::Page as |page|>

  <page.breadcrumbs>
    <BreadCrumbs />
  </page.breadcrumbs>

  <page.header>
    <h2>{{t "resources.roles"}}</h2>
    <p>{{t "resources.descriptions.roles"}}</p>
  </page.header>

  <page.actions>
    {{#if @model}}
      <Rose::LinkButton @route="scopes.scope.roles.new" @style="primary">{{t 'titles.new'}}</Rose::LinkButton>
    {{/if}}
  </page.actions>

  <page.body>

    {{#if @model}}

      <Rose::Table as |table|>
        <table.header as |header|>
          <header.row as |row|>
            <row.headerCell>{{t "form.name.label"}}</row.headerCell>
            <row.headerCell>{{t "form.id.label"}}</row.headerCell>
          </header.row>
        </table.header>
        <table.body as |body|>
          {{#each @model as |role|}}
            <body.row as |row|>
              <row.headerCell>
                <LinkTo
                  @route="scopes.scope.roles.role"
                  @model={{role.id}}
                >
                  {{role.displayName}}
                </LinkTo>
                <p>{{role.description}}</p>
              </row.headerCell>
              <row.cell>
                <code>{{role.id}}</code>
              </row.cell>
            </body.row>
          {{/each}}
        </table.body>
      </Rose::Table>

    {{else}}

      <Rose::Layout::Centered>
        <Rose::Message @title={{t 'titles.roles-welcome'}} as |message|>
          <message.description>
            {{t 'descriptions.roles-welcome'}}
          </message.description>
          <message.link @route="scopes.scope.roles.new">
            <Rose::Icon @name="plus-circle-outline" />
            {{t 'titles.new'}}
          </message.link>
        </Rose::Message>
      </Rose::Layout::Centered>

    {{/if}}

  </page.body>

</Rose::Layout::Page>
