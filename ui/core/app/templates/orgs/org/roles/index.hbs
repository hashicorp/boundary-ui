<Rose::Layout::Page as |page|>

  <page.breadcrumbs>
    <BreadCrumbs />
  </page.breadcrumbs>

  <page.header>
    <h2>{{t "resources.roles"}}</h2>
  </page.header>

  <page.actions>
    {{#if @model}}
      <Rose::LinkButton @route="orgs.org.roles.new" @style="primary">{{t 'titles.new'}}</Rose::LinkButton>
    {{/if}}
  </page.actions>

  <page.body>

    {{#if @model}}

      <Rose::Table as |table|>
        <table.header as |header|>
          <header.row as |row|>
            <row.cell>{{t 'form.label.name'}}</row.cell>
            <row.cell>{{t 'form.label.description'}}</row.cell>
          </header.row>
        </table.header>
        <table.body as |body|>
          {{#each @model as |role|}}
            <body.row as |row|>
              <row.cell>
                <LinkTo
                  @route="orgs.org.roles.role"
                  @model={{role}}
                >
                  {{role.name}}
                </LinkTo>
              </row.cell>
              <row.cell>{{role.description}}</row.cell>
            </body.row>
          {{/each}}
        </table.body>
      </Rose::Table>

    {{else}}

      <Rose::Layout::Centered>
        <Rose::Message @title={{t 'titles.roles-welcome'}} as |message|>
          <message.description>
            {{t 'descriptions.roles-welcome'}}
          </message.description>
          <message.link @route="orgs.org.roles.new">
            <Rose::Icon @name="plus-circle-outline" />
            {{t 'titles.new'}}
          </message.link>
        </Rose::Message>
      </Rose::Layout::Centered>

    {{/if}}

  </page.body>

</Rose::Layout::Page>
